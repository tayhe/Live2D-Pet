[project]
name = "live2d-ai-companion"
version = "0.1.0"
description = "MCP Server for controlling Live2DViewerEX via AI Agent"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "websockets>=12.0",
    "pyyaml>=6.0",
]

[project.scripts]
live2d-mcp = "src.server:main"
